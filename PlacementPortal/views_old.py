from django.shortcuts import render

# Create your views here.

# path('student/', StudentList.as_view(), name="student_list"),
# path('student/<int:pk>/marks/', MarkList.as_view(), name="student_marks"),
# path('student/<int:pk>/edit/', StudentUpdate.as_view(), name="student_edit"),
# path('student/<int:pk>/delete/', name="student_delete"),
